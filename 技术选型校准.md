# 技术选型校准 — AI桌面宠物Agent MVP

> 基于 MVP-CutLine.md | 2026-05-30

---

## 校准原则

原方案（v1.1）的技术栈面向全阶段设计，包含了大量 MVP 不需要的组件（Cognee / Letta / LanceDB / Kuzu / PaddleOCR / Whisper.cpp）。本次校准只回答一个问题：**MVP 最少需要哪些技术，每个选型的具体理由和风险是什么。**

原方案中以下组件 **MVP 阶段全部不用**，不进入选型讨论：

| 组件 | 原方案选择 | MVP 不用它的原因 | 何时引入 |
|------|-----------|-----------------|---------|
| Cognee | 知识引擎 | MVP 无知识簇，只做摘要 | Phase 3 |
| Letta (MemGPT) | 记忆框架 | MVP 核心记忆是 JSON，不需要框架 | Phase 2 评估 |
| LanceDB / ChromaDB | 向量 DB | MVP 不做向量检索 | Phase 2 |
| Kuzu / Neo4j | 图 DB | MVP 无知识图谱 | Phase 3 |
| PaddleOCR | OCR | MVP 不支持图片 | Phase 1.5 |
| Whisper.cpp | 本地 ASR | MVP 无语音 | Phase 2 |
| Edge-TTS / CosyVoice | TTS | MVP 无语音 | Phase 2 |
| Ollama / llama.cpp | 本地 LLM | MVP 仅云端 API | Phase 1.5 |
| nomic-embed-text | 嵌入模型 | MVP 不做向量化 | Phase 2 |
| MCP SDK | 工具扩展 | MVP 无工具调用 | Phase 5 |

---

## MVP 技术栈（逐项决策）

### 1. 桌面框架：Tauri 2

**决策：保留 Tauri 2，不切换到 Electron。**

| 维度 | Tauri 2 | Electron |
|------|---------|----------|
| 安装包体积 | ~5-10MB | ~80-150MB |
| 内存占用（idle） | ~30-50MB | ~80-150MB |
| 原生 API 访问 | Rust 后端，能力强 | Node.js，也不错 |
| 透明窗口 | 支持 | 支持 |
| 鼠标穿透 | 支持（set_ignore_cursor_events） | 支持 |
| 文件拖拽 | 有 API，需验证细节 | 成熟 |
| 社区/生态 | 较小但在增长 | 非常成熟 |

**保留理由**：桌面宠物是常驻应用，体积和内存是硬约束。Tauri 在这两点上有结构性优势。Electron 的成熟度优势在 MVP 阶段不构成决定性差异。

**MVP 需要用到的 Tauri 2 API**：

```
tauri::window          → 透明窗口、置顶、鼠标穿透、窗口位置记忆
tauri::drag_drop       → 文件拖拽接收（核心功能）
tauri::fs              → 读写核心记忆 JSON 文件
tauri::path            → 应用数据目录
tauri::shell           → （可能）打开外部文件/链接
tauri::tray            → 系统托盘图标
tauri::global_shortcut → 全局快捷键唤起（可选）
```

**风险项**：
- `drag_drop` 事件在不同 OS 上的行为差异 → Spike 验证
- `set_ignore_cursor_events` 与 Live2D 渲染的兼容性 → Spike 验证
- Tauri 2 的 macOS 公证和 Windows 签名流程 → 发布前处理

---

### 2. 前端框架：React + TypeScript + Vite

**决策：保留。**

| 选项 | 评估 |
|------|------|
| React 18 + TypeScript | 生态最大，Live2D 集成库基于 React/PixiJS，无明显替代必要 |
| Vue 3 | 原方案备选，但 Live2D 社区资源少于 React，不切换 |
| Svelte / Solid | 性能好体积小，但生态太小，Live2D 无现成集成方案 |
| Vite | Tauri 2 官方推荐构建工具，无替代必要 |

**状态管理**：MVP 用 React Context + useReducer 即可。不上 Zustand / Redux。

---

### 3. 宠物渲染：pixi-live2d-display

**决策：MVP 使用 pixi-live2d-display（开源 Live2D 渲染器），不使用官方 Cubism SDK。**

| 选项 | 许可证 | 优劣 |
|------|--------|------|
| **pixi-live2d-display** | MIT | 开源免费，基于 PixiJS，社区活跃，支持 Cubism 2/3/4 模型格式。API 简洁，可直接控制动画切换 |
| Cubism Web SDK（官方） | Live2D 商用许可 | 官方支持，功能完整，但商用需签许可协议（免费用于小团队/个人，年收入 <1000万日元） |
| Spine | 商用付费 | 骨骼动画方案，非 Live2D 生态，MVP 无必要 |
| CSS Sprite 动画 | 免费 | 最简单但表现力最差，作为 Live2D 不可行时的 fallback |

**选择 pixi-live2d-display 的理由**：
- MIT 许可证，无商用限制
- API 已封装好模型加载、动画切换、鼠标交互
- PixiJS 渲染性能好，GPU 加速
- 支持 Cubism 4（当前主流格式）

**技术验证要点**：
```javascript
// pixi-live2d-display 核心 API（预期用法）
import { Live2DModel } from 'pixi-live2d-display';

const model = await Live2DModel.from('model.model3.json');
app.stage.addChild(model);

// 动画切换（MVP 只需 3 种）
model.motion('idle');       // idle 状态
model.motion('thinking');   // 处理中
model.motion('done');       // 完成
```

**风险**：
- pixi-live2d-display 非官方维护，Cubism 新版本兼容性可能滞后
- 需要确保模型有对应的 thinking/done 动画组，否则需要用 expression 组合模拟

**Fallback 方案**：如果 pixi-live2d-display 集成遇到阻塞问题，降级为 CSS Sprite 动画（准备 3 组 PNG 序列帧），牺牲表现力换取可控性。

---

### 4. 文件解析

**决策：前端 Web Worker 中处理，不上后端 Rust 解析。**

| 文件类型 | 库 | 说明 |
|---------|-----|------|
| .txt | 原生 FileReader API | 零依赖 |
| .md | marked（轻量） | 仅用于提取纯文本，不需要渲染 HTML |
| .pdf | pdfjs-dist | Mozilla 出品，在 Web Worker 中运行，提取纯文本 |

**为什么不用 Rust 后端解析**：MVP 只处理纯文本类文件，前端完全能搞定。Rust 后端留给 Phase 1.5 处理 Word/Excel 等二进制格式时再引入。

**PDF 处理流程**：
```
拖入 .pdf 文件
  → Tauri drag_drop 事件获取文件路径
  → 读取文件为 ArrayBuffer
  → pdfjs-dist 在 Web Worker 中逐页提取文本
  → 拼接为完整文本
  → 送入 LLM 处理
```

**大文件策略**：
- 文本 < 8000 tokens：直接送 LLM
- 文本 8000-40000 tokens：Map-Reduce 分块摘要（每块独立摘要 → 合并再摘要）
- 文本 > 40000 tokens：仅取前 40000 tokens + 提示用户文件被截断

Token 计数用 `js-tiktoken`（OpenAI tokenizer 的 JS 实现）。

---

### 5. LLM 接入

**决策：直接调用 OpenAI 兼容 API，不使用 LangChain 或任何中间框架。**

| 选项 | 评估 |
|------|------|
| 直接 fetch / openai npm 包 | 最轻量，零依赖，MVP 只需要 chat completion |
| LangChain.js | 过度封装，MVP 不需要 chain/agent/tool 抽象 |
| Vercel AI SDK | 不错但绑定 Next.js 生态，Tauri 无必要 |

**使用 `openai` npm 包**（官方 SDK），配置 baseURL 即可兼容所有 OpenAI 格式 API（OpenAI / DeepSeek / Moonshot / 通义千问等）。

```typescript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: userConfig.apiKey,
  baseURL: userConfig.baseURL, // 用户自定义，兼容各种后端
});

const response = await client.chat.completions.create({
  model: userConfig.model,      // 用户自选模型
  messages: [
    { role: 'system', content: systemPrompt },
    { role: 'system', content: coreMemoryInjection },
    ...conversationHistory,
  ],
});
```

**System Prompt 结构（MVP）**：
```
[角色设定] 你是{name}，一只桌面宠物。{personality_description}
[核心记忆] 用户叫{user_name}。{user_note}。你最近帮用户处理过：{recent_files_summary}
[行为指令] 用户给你投喂了文件，请用3-5句话总结核心内容。语气保持{style}。
```

MVP 的 system prompt 就这三段。不需要复杂的 few-shot 或 chain-of-thought。

---

### 6. 数据存储

**决策：JSON 文件，Tauri fs API 读写，不上数据库。**

| 数据 | 存储方式 | 路径 |
|------|---------|------|
| 核心记忆 | core_memory.json | {appDataDir}/core_memory.json |
| 用户设置 | settings.json | {appDataDir}/settings.json |
| 窗口位置 | window_state.json | {appDataDir}/window_state.json |

MVP 阶段三个 JSON 文件搞定所有持久化。不引入 SQLite / IndexedDB / 任何数据库。

---

### 7. 项目结构

```
ai-desktop-pet/
├── src-tauri/                # Rust 后端
│   ├── src/
│   │   ├── main.rs           # Tauri 入口
│   │   ├── window.rs         # 透明窗口 + 鼠标穿透
│   │   └── drag_drop.rs      # 文件拖拽处理
│   ├── Cargo.toml
│   └── tauri.conf.json
├── src/                      # Web 前端
│   ├── main.tsx              # React 入口
│   ├── App.tsx               # 主组件
│   ├── components/
│   │   ├── Pet.tsx           # Live2D 渲染 + 动画控制
│   │   ├── Bubble.tsx        # 对话气泡
│   │   ├── InputBox.tsx      # 文本输入框
│   │   ├── ContextMenu.tsx   # 右键菜单
│   │   └── Settings.tsx      # 设置面板
│   ├── services/
│   │   ├── llm.ts            # OpenAI API 调用
│   │   ├── fileParser.ts     # 文件解析（txt/md/pdf）
│   │   └── memory.ts         # 核心记忆读写
│   ├── hooks/
│   │   └── usePetState.ts    # 宠物状态管理（idle/thinking/done）
│   └── prompts/
│       └── system.ts         # System prompt 模板
├── public/
│   └── models/               # Live2D 模型文件
│       └── pet/
│           ├── pet.model3.json
│           ├── textures/
│           └── motions/
├── package.json
├── vite.config.ts
└── tsconfig.json
```

**关键依赖清单**：
```json
{
  "dependencies": {
    "@tauri-apps/api": "^2",
    "react": "^18",
    "react-dom": "^18",
    "pixi.js": "^7",
    "pixi-live2d-display": "^0.4",
    "openai": "^4",
    "pdfjs-dist": "^4",
    "js-tiktoken": "^1",
    "marked": "^12"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5",
    "vite": "^5",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  }
}
```

总依赖数：9 个运行时依赖，5 个开发依赖。极简。

---

## 技术 Spike 计划（开发前执行）

按优先级排序，每个 Spike 独立验证，失败则触发备选方案：

### Spike 1：Tauri 2 透明窗口 + 鼠标穿透（阻塞级）

**验证目标**：在 Windows/macOS 上实现一个 Tauri 2 窗口，背景透明，非宠物区域点击穿透到桌面。

**验收标准**：
- [ ] 窗口无标题栏、无背景、置顶显示
- [ ] 点击宠物区域可以拖拽移动窗口
- [ ] 点击非宠物区域（透明部分）穿透到下方应用
- [ ] 不闪烁、不白屏

**失败备选**：Electron（transparent: true + setIgnoreMouseEvents）

### Spike 2：Tauri 2 文件拖拽接收（阻塞级）

**验证目标**：从系统文件管理器拖拽一个文件到 Tauri 窗口，获取文件路径和文件内容。

**验收标准**：
- [ ] 拖拽 .pdf/.txt/.md 文件到窗口，触发 drag_drop 事件
- [ ] 事件中可获取文件绝对路径
- [ ] 通过路径读取文件内容（二进制/文本）

**失败备选**：改用 Tauri 系统对话框选择文件（体验降级但仍可用）

### Spike 3：pixi-live2d-display 在 Tauri 中渲染（重要级）

**验证目标**：在 Tauri WebView 中用 PixiJS + pixi-live2d-display 渲染一个 Live2D 模型，切换动画状态。

**验收标准**：
- [ ] 加载 .model3.json 格式的 Live2D 模型
- [ ] 模型正确显示在透明背景上
- [ ] 通过代码切换 idle / thinking / done 三种动画
- [ ] 帧率稳定 60fps，内存 < 100MB

**失败备选**：CSS Sprite 动画（PNG 序列帧，用 CSS animation 切换）

### Spike 4：PDF 文本提取（一般级）

**验证目标**：用 pdfjs-dist 在 Web Worker 中从 PDF 提取纯文本。

**验收标准**：
- [ ] 中文 PDF 文本正确提取（不乱码）
- [ ] 多页 PDF 拼接为完整文本
- [ ] 100 页以内的 PDF 处理时间 < 3 秒

**失败备选**：Tauri Rust 后端用 pdf-extract crate 处理

### Spike 5：OpenAI API 长文本摘要（一般级）

**验证目标**：验证 Map-Reduce 分块摘要策略在长文本上的效果。

**验收标准**：
- [ ] 8000 tokens 以内的文本直接摘要，结果质量可接受
- [ ] 8000-40000 tokens 的文本 Map-Reduce 摘要，结果连贯
- [ ] 单次摘要 API 调用时间 < 10 秒（GPT-4o-mini）

**失败备选**：Refine 策略（逐步精炼，API 调用次数多但单次快）

---

## 与原方案差异汇总

| 维度 | 原方案 v1.1 | MVP 校准后 | 理由 |
|------|------------|-----------|------|
| Live2D 渲染 | "Live2D" | pixi-live2d-display (MIT) | 开源免费，避免许可证风险 |
| LLM 调用 | OpenAI API + Ollama | 仅 openai npm 包直调 | MVP 无本地 LLM |
| 文件解析 | 未指定 | pdfjs-dist + marked + FileReader | 前端处理，零后端依赖 |
| 向量 DB | LanceDB / ChromaDB | 不用 | MVP 无向量检索 |
| 图 DB | Kuzu | 不用 | MVP 无知识图谱 |
| 知识引擎 | Cognee | 不用 | MVP 无知识簇 |
| 记忆框架 | Letta | 不用，JSON 文件 | MVP 核心记忆极简 |
| OCR | PaddleOCR | 不用 | MVP 无图片处理 |
| ASR/TTS | Whisper.cpp / Edge-TTS | 不用 | MVP 无语音 |
| 状态管理 | 未指定 | React Context + useReducer | 最简方案 |
| Token 计数 | 未指定 | js-tiktoken | 长文本分块需要 |
| 总运行时依赖 | 未统计 | 9 个 | 极简 |
