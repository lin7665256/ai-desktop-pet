- 前端服务层统一通过 `@tauri-apps/api/core` 的 `invoke` 函数调用 Rust 后端指令，实现跨语言通信。
- LLM 交互采用多策略适配机制，根据文本 Token 数量自动选择直接摘要、Map-Reduce 分块摘要或截断摘要。
- 应用状态（Settings/CoreMemory）通过 Rust 后端读写本地 JSON 文件实现持久化，前端仅负责状态同步。
- 宠物动画状态与业务处理阶段（reading/summarizing/done）严格绑定，通过 `useEffect` 监听状态变化触发 Live2D 动作。