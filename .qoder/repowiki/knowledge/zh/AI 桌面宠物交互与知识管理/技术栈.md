- **Tauri 2**: 使用 `tauri::Manager` 和 `TrayIconBuilder` 构建系统托盘与窗口生命周期管理。
- **PixiJS v7 + pixi-live2d-display**: 用于渲染 Cubism 4 格式的 Live2D 宠物模型，支持 idle/tap 等动作切换。
- **OpenAI SDK (Browser)**: 前端直连 LLM API，集成 `js-tiktoken` 进行 Token 计数与分块策略（Direct/Map-Reduce/Truncated）。