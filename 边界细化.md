# "不做什么"边界细化 — AI桌面宠物Agent

> 基于产品定义方案 v1.1 + MVP-CutLine | 2026-05-30

---

## 边界总览

```
本产品做的                          本产品不做的
─────────                          ─────────
桌面端信息快捷投喂+处理              Web端知识库
单用户个人助手                      团队协作/项目管理
宠物形态的AI交互                    通用AI对话产品
"拖一下"的极简入口                  全功能知识管理系统
```

边界判断的一句话标准：**如果一个功能离开桌面/离开宠物形态依然成立，那它大概率不是我们该做的。**

---

## 一、绝对不做（任何阶段都不做）

这些不是"MVP不做"，是产品基因层面不做。做了就是在变成另一个产品。

| 功能 | 不做的原因 |
|------|-----------|
| Web 端知识库 | Notion / 语雀 / ima 的赛道。我们的价值是"桌面快捷入口"，做 Web 端等于自己革自己的命 |
| 团队协作（共享知识库/权限/多人编辑） | 团队协作需要 Web 端、权限系统、冲突解决——每一项都是独立产品的复杂度。我们是单用户桌面工具 |
| 项目管理（看板/任务/甘特图） | 飞书/Jira/Linear 的赛道。宠物可以"帮你记个待办"，但不做项目管理界面 |
| 多宠物（一个用户养多只） | 增加大量复杂度（记忆隔离/资源占用），且情感价值不如一只养得深。一只就够 |
| 宠物间社交（宠物找别人的宠物玩） | 伪需求。桌面宠物是私人助手，不是社交平台 |
| 笔记编辑器（富文本/Markdown 编辑） | Obsidian/Notion 的赛道。宠物帮你"读"和"整理"，不帮你"写" |
| 全文搜索界面（搜索框+结果列表） | 直接问宠物就是搜索。做搜索界面等于承认对话交互失败 |
| 手机端 App | 桌面宠物的核心价值是"住在桌面上=零启动成本"。手机上有手机上的形态（小组件/通知），那是另一个产品 |
| 浏览器作为主界面 | 我们是桌面应用，不是浏览器扩展。浏览器插件只是投喂渠道之一，不是主界面 |

---

## 二、MVP 不做、后续阶段做（明确排期）

| 功能 | MVP 不做的原因 | 进入阶段 | 进入条件 |
|------|--------------|---------|---------|
| 本地 LLM（Ollama） | 增加配置复杂度，早期用户可接受在线 | Phase 1.5 | MVP 验证通过后 |
| Word/Excel/图片文件支持 | MVP 只验证"拖一下"的动作，格式覆盖后补 | Phase 1.5 | 拖拽交互验证通过 |
| 完整 6+ 动画状态 | 3 种够用，完整状态机是锦上添花 | Phase 2 | — |
| 情绪系统 | 纯表达层增强，不影响核心功能 | Phase 2 | — |
| 精力系统 | 同上 | Phase 2 | — |
| 召回记忆（向量检索） | 核心记忆够 MVP 用，向量检索是记忆深度的质变 | Phase 2 | 用户使用频次 > 每周 3 次 |
| 语音输入/输出 | 非核心交互通道，增加依赖 | Phase 2 | — |
| 剪贴板自动捕获 | 隐私争议 + MVP 已有拖拽和双击两个入口 | Phase 2 | 用户反馈强烈需要 |
| 知识簇自动构建（Cognee） | MVP 做摘要不做聚类，知识簇是留存引擎 | Phase 3 | 用户累积 > 50 份投喂资料 |
| 主动浮现（Heads Up） | 没有知识簇基础，浮现无内容支撑 | Phase 3 | 知识簇可用后 |
| 知识簇可视化（地图/图谱） | 列表视图先够用，可视化是高级功能 | Phase 3 | — |
| 浏览器插件 | 投喂渠道扩展 | Phase 4 | 云端同步可用后 |
| 微信/飞书分享到宠物 | 第三方集成 | Phase 4 | — |
| 云端同步 | MVP 纯本地，同步是跨设备需求驱动 | Phase 4 | 用户有第二台设备的需求 |
| MCP 工具扩展 | Agent 能力扩展 | Phase 5 | Agent 基础能力稳定后 |
| Sleep-time Compute | 需要记忆数据积累到一定量 | Phase 5 | — |
| 文件夹自动监控 | 自动摄入指定文件夹新文件 | Phase 2 | — |

---

## 三、功能深度边界（做到什么程度就停）

这是最容易被忽视的部分。不是"做不做"的问题，是"做到哪里停"的问题。

### 3.1 投喂功能

| 做到这里 | 不再往下做 |
|---------|-----------|
| 拖文件到宠物 → 解析 → 气泡摘要 | ❌ 不做投喂后的编辑/标注/批注 |
| 摘要结果在气泡中展示 | ❌ 不做独立的"投喂历史"页面（MVP） |
| 最近投喂的文件名记录在核心记忆 | ❌ 不做投喂统计/分析仪表盘 |
| 单次最多投喂 1 个文件（MVP） | ❌ MVP 不做批量拖拽队列 |
| 支持 txt/md/pdf | ❌ MVP 不做 Word/Excel/图片/音频/视频 |

**Phase 2 扩展**：批量投喂、更多格式、投喂历史记录列表。
**绝不做**：投喂后的富文本编辑、标注工具、批注系统。那是阅读器的活。

### 3.2 对话功能

| 做到这里 | 不再往下做 |
|---------|-----------|
| 双击宠物 → 输入框 → 气泡回复 | ❌ 不做独立的对话窗口/全屏模式（MVP） |
| 当前会话保留对话历史 | ❌ 不做跨会话对话历史浏览界面 |
| 基于最近投喂文件的追问 | ❌ 不做"选择文件进行对话"的显式UI |
| 气泡支持纯文本 + 基础 Markdown | ❌ 不做代码高亮/表格/图表渲染（MVP） |

**Phase 2 扩展**：全屏对话模式、对话历史浏览、代码块渲染。
**绝不做**：对话导出/分享、多轮对话管理界面。用户想保存重要对话，自己复制到笔记工具里。

### 3.3 记忆功能

| 做到这里 | 不再往下做 |
|---------|-----------|
| 核心记忆：用户名/偏好/宠物设定/最近文件 | ❌ 不做记忆管理界面（MVP） |
| 宠物在对话中体现"记得你" | ❌ 不做"查看宠物记住了什么"的透明度面板（MVP） |
| JSON 文件持久化 | ❌ 不做记忆版本历史/回滚 |
| Agent 自主更新核心记忆 | ❌ 不做用户手动编辑记忆的界面（Phase 2+） |

**Phase 2 扩展**：记忆查看面板（让用户看到宠物记了什么，可以纠正）、向量召回记忆。
**绝不做**：记忆时间线可视化、记忆关系图谱。记忆的价值是"用"，不是"看"。

### 3.4 知识簇功能（Phase 3+）

| 做到这里 | 不再往下做 |
|---------|-----------|
| 自动归类到知识簇 | ❌ 不做手动打标签/建文件夹 |
| 两层簇结构（领域→子主题） | ❌ 不做三层以上嵌套 |
| 列表视图查看知识簇 | ❌ MVP 不做图谱/地图可视化 |
| 基于知识簇回答提问 | ❌ 不做"在某个簇内搜索"的专用界面 |

**Phase 3+ 扩展**：知识簇地图可视化、手动合并/拆分簇。
**绝不做**：知识库广场（分享/发现别人的知识簇）——至少 Phase 5 之前不做。分散精力，且涉及内容审核问题。

### 3.5 宠物形象

| 做到这里 | 不再往下做 |
|---------|-----------|
| 1 个默认 Live2D 模型 | ❌ MVP 不做形象选择/切换 |
| 给宠物取名字 | ❌ 不做换装/换色/配件系统（MVP） |
| 3 种动画状态 | ❌ MVP 不做动画品质调节 |
| 宠物在桌面上可拖拽移动 | ❌ MVP 不做跟随鼠标/边缘栖息/多显示器 |

**Phase 2 扩展**：形象选择（2-3 个预设）、换装系统。
**绝不做**：用户自建宠物形象上传、形象交易市场。UGC 生态运营成本远超预期。

### 3.6 主动浮现（Phase 3+）

| 做到这里 | 不再往下做 |
|---------|-----------|
| 气泡轻提示 + 3 秒自动消失 | ❌ 不做强制弹窗/不可关闭的通知 |
| 三档频率调节（低/中/高） | ❌ 不做"自定义触发规则"的高级设置 |
| 免打扰模式 | ❌ 不做定时免打扰/场景感知免打扰（MVP） |
| 浮现历史记录（可查看） | ❌ 不做浮现数据的分析/统计 |

**绝不做**：广告推送、商业推荐、任何形式的付费浮现。这是底线。

---

## 四、平台边界

| 平台 | 做不做 | 说明 |
|------|--------|------|
| Windows 10/11 | ✅ MVP | 首要平台，用户基数最大 |
| macOS | ✅ MVP | Tauri 2 天然跨平台，同步支持 |
| Linux | ⚠️ 不主动适配 | Tauri 2 支持 Linux，但不做专门测试和适配。用户能跑就跑 |
| Android | ❌ | 桌面宠物形态不适用于移动端 |
| iOS | ❌ | 同上 |
| Web 浏览器 | ❌ | 我们是桌面应用 |
| ChromeOS | ❌ | 太小众 |

---

## 五、用户类型边界

| 用户类型 | 是否服务 | 说明 |
|---------|---------|------|
| 个人开发者/信息工作者 | ✅ 核心用户 | 每天处理大量碎片信息 |
| 学生/研究者 | ✅ 扩展用户 | 需要喂资料、快速检索 |
| 团队/企业用户 | ❌ | 团队协作不在产品范围内 |
| 儿童/教育场景 | ❌ | 不做内容安全审核、不做儿童模式 |
| 非技术用户（不会配 API Key） | ⚠️ Phase 2+ 解决 | MVP 要求用户自带 API Key，后续提供"开箱即用"方案 |

---

## 六、集成边界

| 集成对象 | MVP | Phase 2+ | 绝不做 |
|---------|-----|----------|--------|
| OpenAI API | ✅ | ✅ | — |
| DeepSeek / Moonshot / 通义千问等 OpenAI 兼容 API | ✅ | ✅ | — |
| Ollama 本地模型 | ❌ | ✅ | — |
| 微信 | ❌ | ❌ | ✅（审核/封号风险极高） |
| 飞书 | ❌ | ✅（Phase 4） | — |
| Slack | ❌ | ✅（Phase 5） | — |
| Notion | ❌ | ✅（Phase 5，读取 Notion 数据） | ❌（写入/编辑 Notion） |
| Obsidian | ❌ | ✅（Phase 2，监控 vault 文件夹） | ❌（嵌入 Obsidian） |
| 浏览器插件 | ❌ | ✅（Phase 4） | — |
| Raycast / Alfred | ❌ | ✅（Phase 2，快捷入口） | — |
| 日历（系统/Google） | ❌ | ✅（Phase 5） | — |
| 邮件 | ❌ | ❌ | ✅（不做邮件客户端） |

---

## 七、边界判断决策树

当遇到"这个功能要不要做"的问题时，按以下顺序判断：

```
这个功能离开桌面/宠物形态还成立吗？
  ├── 成立 → 不做。那不是我们的产品。
  └── 不成立 ↓

这个功能是核心交互（拖拽/对话）的增强，还是全新交互通道？
  ├── 全新通道（语音/手势/眼动） → Phase 2+ 再考虑
  └── 核心交互增强 ↓

没有这个功能，核心假设能不能验证？
  ├── 能验证 → 不在 MVP，排入后续阶段
  └── 不能验证 → 进入 MVP
```
