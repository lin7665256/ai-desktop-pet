# MVP Cut Line — AI桌面宠物Agent

> 基于产品定义方案 v1.1 深耕 | 2026-05-30
> v2 修订：定位校准为「不经意知识库」| 2026-05-30

---

## 核心假设（MVP 必须验证）

**用户在"喂宠物"的过程中，不经意间形成了自己的知识库——宠物越用越懂用户，用户越来越离不开。**

"拖一下"是交互手段，"知识库"是自然生长的结果，"宠物"是让这一切不费力的载体。

验证标准：
- 用户在首次体验后，第二天仍会主动使用拖拽投喂（**习惯形成**）
- 用户在投喂 3+ 份文件后，感知到宠物"越来越了解我的关注点"（**知识积累感知**）
- 宠物在第二次打开时，能关联上次投喂的内容来回应（**跨文件关联**）
- 用户感知到"拖一下"比手动复制粘贴到 ChatGPT/浏览器更省事（**效率感知**）

---

## 功能清单：做 / 简化做 / 不做

### 必须做（没有就不是这个产品）

| # | 功能 | 规格 | 为什么必须 |
|---|------|------|-----------|
| 1 | Tauri 透明窗口 | 窗口置顶 + 鼠标穿透 + 可拖拽移动 | 没有这个就没有"桌面宠物" |
| 2 | Live2D 宠物渲染 | 3 个动画状态：idle / thinking / done | 最小可用的"活物感" |
| 3 | 拖拽文件投喂 | 拖文件到宠物 → 解析 → LLM处理 → 气泡输出结果摘要 | **核心卖点，必须在 MVP 验证** |
| 4 | 文本对话 | 双击宠物 → 弹出输入框 → LLM 响应 → 气泡显示 | 投喂之外的基础交互通道 |
| 5 | 核心记忆 | JSON 文件持久化（用户名/偏好/宠物设定），每次对话注入 | 第二次打开还认识你 |
| 6 | 云端 LLM 接入 | OpenAI 兼容 API，用户自带 Key | 响应质量是体验底线 |
| 7 | 右键菜单 | 投喂文件 / 对话 / 设置 / 退出 | 基础操作入口 |
| 8 | 设置面板 | API Key 配置 / 宠物名字 / 基础偏好 | 用户必须能配置 API |

### 简化做（有但只做最小版本）

| # | 功能 | MVP 做到什么程度 | 不做的那部分留给谁 |
|---|------|----------------|-------------------|
| 1 | 动画状态 | 3 种：idle / thinking / done | 完整 6+ 状态留给 Phase 2 |
| 2 | 文件支持 | 仅纯文本类：.txt / .md / .pdf | Word/图片/OCR 留给 Phase 1.5 |
| 3 | 投喂结果 | 气泡显示一段摘要文本 | 结构化卡片/操作按钮留给 Phase 2 |
| 4 | 对话历史 | 仅当前会话保留，关闭即清空 | 召回记忆/向量检索留给 Phase 2 |
| 5 | 宠物位置 | 可拖拽移动，记住上次位置 | 跟随鼠标/边缘模式留给 Phase 2 |
| 6 | 首次引导 | 取名 + 选 API Key + 一句话介绍自己能干嘛 | 完整人格选择/知识导入留给 Phase 2 |

### 明确不做（MVP 红线之外）

| 功能 | 不做的原因 | 什么时候做 |
|------|-----------|-----------|
| 本地 LLM（Ollama） | 增加大量复杂度，MVP 专注核心交互验证 | Phase 1.5 |
| 召回记忆（向量检索） | 核心记忆已够 MVP 验证"认识你" | Phase 2 |
| 知识簇/Cognee | 投喂→摘要→气泡已够 MVP，聚类是留存引擎不是验证项 | Phase 3 |
| 主动浮现（Heads Up） | 没有知识簇基础，浮现没有内容支撑 | Phase 3 |
| 情绪系统 | 3 种动画状态够用，情绪是锦上添花 | Phase 2 |
| 精力系统 | 纯表达层，不影响核心体验验证 | Phase 2 |
| 语音输入/输出 | 非核心交互，增加依赖和复杂度 | Phase 2 |
| 剪贴板自动捕获 | 容易引发隐私争议，且 MVP 有拖拽和双击两个入口 | Phase 2 |
| 知识簇可视化 | 没有知识簇就没有可视化的对象 | Phase 3 |
| 浏览器插件 | 投喂渠道扩展，MVP 验证桌面拖拽就够 | Phase 4 |
| 云端同步 | MVP 纯本地存储，不涉及跨设备 | Phase 4 |
| MCP 工具扩展 | Agent 能力扩展，MVP 只做投喂+对话 | Phase 5 |
| Sleep-time Compute | 需要记忆系统积累足够数据才有意义 | Phase 5 |

---

## MVP 用户旅程（完整路径）

```
下载安装
  ↓
首次启动：宠物出现在桌面右下角
  ↓
右键菜单 → 设置 → 填入 API Key + 给宠物取名
  ↓
宠物气泡："你好，我是{name}。把文件拖给我，我帮你读。"
  ↓
【第一次投喂】用户拖一个 PDF 到宠物身上
  ↓
宠物切换 thinking 动画 → 气泡："在读..."
  ↓
处理完成 → 气泡显示摘要（3-5句话）
  ↓
【第二次投喂】用户拖另一个相关文件
  ↓
宠物处理完 → 气泡摘要 + "跟你之前那份{旧文件名}有关系，都在讲{共同主题}。"
  ← 这是关键瞬间：用户第一次感受到"它在帮我积累知识"
  ↓
用户可以双击宠物追问："这两份有什么异同？"
  ↓
关闭应用 → 第二天打开 → 宠物："又来啦。你最近在研究{interest_signal}，有新东西要我看吗？"
  ← 第二个关键瞬间：宠物记住了用户的关注方向
```

**关键体验断点**：
- 如果拖拽/解析/摘要任何一步卡住 → 效率假设不成立
- 如果第二次投喂时宠物没有关联旧文件 → 知识库假设不成立
- 如果第二天打开宠物不记得用户 → 陪伴假设不成立

---

## MVP 技术验证项（开发前必须 Spike）

在正式开发前，以下技术点需要先验证可行性：

| # | 验证项 | 验证目标 | 预计耗时 |
|---|--------|---------|---------|
| 1 | Tauri 2 透明窗口 + 鼠标穿透 | 能实现桌面常驻 + 非宠物区域点击穿透 | 1-2 天 |
| 2 | Tauri 2 文件拖拽接收 | 能从系统文件管理器拖文件到 Tauri 窗口并获取文件路径 | 1 天 |
| 3 | pixi-live2d-display 集成 | 在 Tauri Web 视图中渲染 Live2D 模型 + 切换动画 | 2-3 天 |
| 4 | PDF 文本提取 | 用 pdf.js 或 pdf-parse 从 PDF 提取纯文本 | 0.5 天 |
| 5 | OpenAI API 长文本处理 | 处理超长文本的分块摘要策略（Map-Reduce 或 Refine） | 1 天 |

**如果 Spike 1 或 2 不可行，需要重新评估桌面框架选型。**

---

## 性能预算（MVP 硬约束）

桌面宠物是常驻应用，性能超标会直接被用户关掉。这些约束从 MVP 开始就要遵守：

| 指标 | 上限 | 理由 |
|------|------|------|
| 内存占用 | < 150MB | 常驻应用不能超过一个浏览器 Tab |
| CPU 占用（idle） | < 3% | 宠物待机时几乎不应消耗 CPU |
| CPU 占用（处理中） | < 30% | 处理任务时可以有峰值，但不能卡系统 |
| 启动时间 | < 2s | 开机自启时不能让用户等 |
| 安装包体积 | < 50MB | 下载门槛要低 |
| 窗口渲染延迟 | < 16ms (60fps) | 动画不能卡顿 |

---

## 投喂流程规格（MVP 最小版本）

虽然投喂交互规格书后续单独出，但 MVP 的最小流程需要在此定义：

### 支持的文件类型（MVP）

| 类型 | 格式 | 处理方式 |
|------|------|---------|
| 纯文本 | .txt, .md | 直接读取文本内容 |
| PDF | .pdf | pdf.js 提取文本 → LLM 处理 |

### 投喂状态流

```
文件进入宠物感应区域
  → 宠物目光跟随文件（如有对应动画）
  → 文件松手在宠物身上
  → 宠物切换 thinking 动画
  → 气泡："在处理 {文件名}..."
  → LLM 处理（Pass 1：摘要 + 主题提取 + 关联检测）
  → 宠物切换 done 动画
  → 气泡显示结果摘要
  → 后台更新核心记忆（Pass 2：更新 interest_signal）
  → 5秒后气泡淡出，回到 idle
```

### 投喂后处理流程（LLM 调用策略）

每次投喂文件，LLM 被调用两次。两次调用各司其职：

**Pass 1：摘要+主题+关联（用户可见，需要快）**

```
System: 你是{name}，桌面宠物。{人格设定}。
User: 用户给你投喂了文件「{文件名}」。
      文件内容：{文本内容}
      用户之前投喂过的文件：{recent_files 摘要列表}
      请用3-5句话总结这份文件的核心内容。
      如果和之前的文件有主题关联，在摘要末尾提一句。
      最后，输出JSON格式的元数据：
      {"topics": ["主题1", "主题2"], "related_file": "关联的旧文件名或null"}
```

输出解析：
- 摘要文本 → 显示在气泡中
- topics 数组 → 用于 Pass 2 更新核心记忆
- related_file → 如果非 null，在气泡摘要末尾展示关联

**Pass 2：兴趣画像更新（后台静默，不影响用户感知速度）**

```
System: 你是一个数据分析助手。
User: 当前用户兴趣画像：{interest_signal}
      当前主题分布：{topics}
      新投喂文件的主题：{新提取的topics}
      请更新用户的兴趣画像，输出一句话描述。
      只输出更新后的 interest_signal，不要其他内容。
```

输出：更新后的 interest_signal 字符串 → 写入 core_memory.json

**为什么分两次而不是合并为一次**：
- Pass 1 需要快（用户在看气泡等结果），输出要有人格化的语气
- Pass 2 需要结构化（更新 JSON 数据），不需要人格化
- 分开后 Pass 2 可以异步执行，不阻塞气泡显示
- Pass 2 失败不影响用户体验（最多是 interest_signal 不更新，下次补上）

### 异常处理（MVP）

| 场景 | 处理方式 |
|------|---------|
| 文件格式不支持 | 气泡："这个格式我暂时读不了，目前支持 txt/md/pdf" |
| 文件过大（>20MB） | 气泡："这个文件太大了，我啃不动" |
| 文本提取为空 | 气泡："这个文件好像没有文字内容" |
| LLM 调用失败 | 气泡："处理出了点问题，要不要再试一次？" |
| 网络断开 | 气泡："断网了，我没法思考..." + 恢复后自动重试 |
| Pass 2 失败 | 静默失败，不影响用户。下次投喂时重试更新 |

---

## 隐私与数据安全策略（MVP）

桌面宠物处理用户的文件内容，隐私是硬约束。以下策略从 MVP 开始执行：

### 数据流向

```
用户文件（本地）
  → 文本提取（本地，Web Worker）          ← 文件内容不离开本机
  → 提取的文本（发送到 LLM API）           ← 唯一的外发环节
  → LLM 返回摘要+主题（存储到本地 JSON）   ← 结果只存本地
  → 气泡显示给用户（本地渲染）             ← 不经过任何中间服务器
```

### 用户必须知道的事

| 事项 | MVP 处理方式 |
|------|-------------|
| 文件内容是否发送给第三方 | **是**。文件文本会发送到用户配置的 LLM API（如 OpenAI）。必须在设置面板明确告知 |
| API Key 存在哪里 | 仅存本地 settings.json，不经过任何中间服务器 |
| 核心记忆存在哪里 | 仅存本地 core_memory.json |
| 是否有遥测/埋点 | MVP 不做。如需后续添加，必须 opt-in |
| 数据能否导出 | MVP 暂不做导出 UI，但数据就是 JSON 文件，用户可以直接查看 |
| 删除数据 | 删除应用数据目录即可完全清除 |

### 设置面板中的隐私提示（MVP 必须有）

```
⚠️ 你的文件内容会发送到你配置的 LLM API 进行处理。
   API Key 仅存储在本地，不经过第三方服务器。
   核心记忆和投喂记录仅存储在本地。
```

### 不做的事

- ❌ 不收集用户使用数据
- ❌ 不上传文件到自有服务器
- ❌ 不做遥测/埋点（MVP）
- ❌ 不存储 API Key 到云端

---

## 数据模型（MVP 最小版本）

### 核心记忆（core_memory.json）

```json
{
  "pet": {
    "name": "用户取的宠物名",
    "created_at": "首次启动时间"
  },
  "user": {
    "name": "用户的名字/昵称",
    "note": "用户自述的一句话简介"
  },
  "knowledge_profile": {
    "topics": [
      { "name": "AI工具", "feed_count": 5, "last_fed": "2026-05-30" },
      { "name": "半导体", "feed_count": 3, "last_fed": "2026-05-28" }
    ],
    "interest_signal": "用户对AI工具和半导体领域关注较多"
  },
  "stats": {
    "total_feeds": 8,
    "total_chats": 12,
    "first_seen": "2026-05-25",
    "last_active": "2026-05-30"
  },
  "recent_files": [
    {
      "name": "Qwen3技术报告.pdf",
      "fed_at": "2026-05-30T14:00",
      "summary": "Qwen3模型架构改进，采用MoE...",
      "topics": ["AI工具", "大模型"]
    },
    {
      "name": "光刻机产业链分析.pdf",
      "fed_at": "2026-05-29T10:00",
      "summary": "ASML垄断EUV市场，国产替代...",
      "topics": ["半导体", "光刻机"]
    }
  ]
}
```

与之前的纯记录模型相比，新增 `knowledge_profile`：
- **topics**：每次投喂时 LLM 提取 1-3 个主题标签，累加计数。这是"不经意知识库"的 MVP 形态——用户没有主动归类，但系统自动积累了主题分布
- **interest_signal**：一句话描述用户的兴趣画像，由 LLM 在每次投喂后更新。注入 system prompt 让宠物体现"了解你"
- **recent_files 增加 topics**：每份文件带标签，让宠物在投喂新文件时能关联旧文件（"跟你上次那份 Qwen3 报告有关"）

这仍然是纯 JSON，不需要向量 DB 或知识图谱。LLM 本身负责关联推理。

### 会话上下文（内存，不持久化）

```
- 当前对话的消息列表
- 最近一次投喂的文件摘要（用于追问）
```

---

## MVP 成功指标

| 指标 | 目标 | 衡量什么 | 怎么衡量 |
|------|------|---------|---------|
| 次日留存 | > 40% | 习惯形成 | 第二天还打开应用的用户比例 |
| 投喂完成率 | > 80% | 效率感知 | 拖入文件后，处理成功并查看结果的比例 |
| 投喂→追问率 | > 30% | 信息价值 | 投喂后还会追问的用户比例 |
| **多次投喂率** | **> 25%** | **知识库形成** | **投喂 3+ 份文件的用户比例（说明在积累，不是尝鲜）** |
| **跨文件关联感知** | **> 50%** | **知识积累感知** | **投喂相关文件后，用户注意到宠物在做关联的比例（问卷）** |
| 崩溃率 | < 2% | 稳定性 | 应用非正常退出的比例 |
| 用户一句话反馈 | "它越来越懂我" | 综合定位验证 | 简单问卷：你觉得这个产品最吸引你的是什么？ |

---

## 与 Phase 2 的分界线

MVP 做完后，Phase 2 的第一件事不是加功能，而是：
1. 收集 MVP 用户反馈
2. 验证三个假设是否成立：
   - **效率假设**（拖一下比自己整理快）→ 看投喂完成率
   - **知识库假设**（用户在不经意间积累了知识）→ 看多次投喂率 + 关联感知
   - **陪伴假设**（宠物形态让用户愿意持续使用）→ 看次日留存
3. 根据验证结果决定 Phase 2 方向：
   - 知识库假设最强 → Phase 2 加深记忆系统 + 知识簇
   - 陪伴假设最强 → Phase 2 加情绪系统 + 更多人格表达
   - 效率假设最强 → Phase 2 加更多文件格式 + 批量投喂
   - 全部偏弱 → 重新审视定位

**不要在 MVP 未验证时就开始建 Phase 2。**
